{
  "name": "mcserverless",
  "version": "1.0.0",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9",
    "npm": "please-use-pnpm",
    "yarn": "please-use-pnpm"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "check-types": "turbo run check-types",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "verify": "pnpm --filter @mcbroken/db db:generate && turbo run check-types lint test",
    "check-api": "ts-node --transpile-only scripts/check-api-health.ts",
    "check-api:us": "ts-node --transpile-only scripts/check-api-health.ts us",
    "check-api:europe": "ts-node --transpile-only scripts/check-api-health.ts europe",
    "check-api:australia": "ts-node --transpile-only scripts/check-api-health.ts australia",
    "invoke": "./scripts/invoke-local.sh",
    "deploy": "pnpm deploy:dev",
    "deploy:dev": "turbo run deploy:dev --filter=./apps/mc*",
    "deploy:staging": "pnpm deploy:dev",
    "deploy:production": "pnpm deploy:dev",
    "deploy:mcall": "turbo run deploy:dev --filter=@mcbroken/mcall",
    "deploy:mcus": "turbo run deploy:dev --filter=@mcbroken/mcus",
    "deploy:mcau": "turbo run deploy:dev --filter=@mcbroken/mcau"
  },
  "devDependencies": {
    "@mcbroken/eslint-config": "workspace:*",
    "@mcbroken/typescript-config": "workspace:*",
    "@types/aws-lambda": "^8.10.161",
    "@types/node": "22",
    "esbuild": "0.25.0",
    "husky": "^8.0.3",
    "prettier": "^3.8.1",
    "serverless": "3.40.0",
    "serverless-esbuild": "1.57.0",
    "serverless-offline": "14.4.0",
    "ts-node": "10.9.2",
    "turbo": "^2.8.16",
    "typescript": "^5.9.3"
  },
  "license": "MIT",
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{md,json,yml,yaml,html}": [
      "prettier --write"
    ]
  },
  "packageManager": "pnpm@10.25.0"
}
